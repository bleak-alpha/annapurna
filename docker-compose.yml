services:
  database:
    image: gvenzl/oracle-xe:21-slim
    container_name: annapurna_database
    environment:
      POSTGRES_DB: ${DB_NAME:-annapurna_db}
      POSTGRES_USER: ${DB_USER:-apps}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-apps}
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - oracle_data:/opt/oracle/oradata
      - ./annapurna_database/00-init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "${DB_PORT:-1521}:1521"
    networks:
      - annapurna-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-annapurna_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  backend:
    build:
      context: ./annapurna-backend
      dockerfile: dockerfile
    container_name: annapurna_backend
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-default}
      DB_NAME: ${DB_NAME:-annapurna_db}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      JAVA_OPTS: "-Xms512m -Xmx1024m"
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    depends_on:
      database:
        condition: service_healthy
    networks:
      - annapurna-net
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  annapurna-net:
    driver: bridge